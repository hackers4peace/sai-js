import { describe, test } from 'vitest'

describe('subdomain with 303 redirect', () => {
  test.todo('request root without subdomain')
  test.todo('request root with subdomain')
  test.todo('request path without subdomain')
  test.todo('request path with subdomain')
})
